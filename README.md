Simple yii2 application with bootstrap, allow to add posts with title(text) and body(HTML), integrated CKEditor, simple file manager page with ajax-uploads.

posts.sql - phpmyadmin backup of table structure.
